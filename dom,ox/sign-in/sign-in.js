const hello = document.querySelector(".header");
const sign = document.querySelector(".body");
hello.addEventListener("click", () => {
  hello.classList("header");
  // sign.classList("body", 2 / 1);
});
